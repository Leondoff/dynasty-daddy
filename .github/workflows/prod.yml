name: Production

on:
  pull_request:
    branches:
      - staging
  push:
    branches:
      - main

jobs:
  build-and-deploy-back:
    name: Build and Deploy Back-end
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          file: ./back-end/express-api/Dockerfile
          push: true
          tags: latest
          image: jmtimper/dynasty-daddy-back-end:latest

  build-and-deploy-front:
    name: Build and Deploy Front-end
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          file: ./front-end/fantasy-app/Dockerfile
          push: true
          tags: latest
          image: jmtimper/dynasty-daddy-front-end:latest

  build-and-deploy-cron:
    name: Build and Deploy Cron
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          file: ./back-end/scripts/KTCScraperScript/Dockerfile
          push: true
          tags: latest
          image: jmtimper/dynasty-daddy-cron:latest
